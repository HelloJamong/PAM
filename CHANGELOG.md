# Changelog

모든 주요 변경 사항은 이 파일에 기록됩니다.
버전 형식: `연도.메이저버전.마이너버전` (예: `26.1.0` = 2026년, 메이저 1, 마이너 0)

## [26.1.5] - 2026-09-03

### Changed
- 데이터 변경 시 자동 백업을 응답을 막지 않고 비동기로 실행하도록 전환 (저장 응답 지연 제거, 백업 트리거·직렬 실행·보존 개수는 동일)
- 백업 상태를 마지막으로 완료된 백업 기준으로 변경 응답과 `/api/health`에 노출하고 실패 안내 문구를 상태에 포함
- 자산·이력 목록의 페이지네이션·검색 debounce 로직을 공통 훅으로 통합하고 CSV 다운로드·날짜 유틸 중복 제거

### Fixed
- 존재하지 않는 `/api/*` 경로가 SPA `index.html`(HTTP 200)로 응답하던 문제를 JSON 404 응답으로 수정

## [26.1.4] - 2026-08-12

### Changed
- Windows CI와 릴리스 빌드에서 런처 전체 테스트를 실행해 Windows 전용 프로세스 설정을 검증하도록 변경

### Fixed
- `PAM.exe` 실행 시 백엔드 `node.exe` 콘솔 창이 별도로 노출되어 사용자가 닫으면 서비스가 중단되던 문제 수정
- Windows 자식 프로세스에 `CREATE_NO_WINDOW`와 숨김 속성을 적용해 Node.js 서버가 시스템 트레이 뒤에서 백그라운드로 실행되도록 수정

## [26.1.3] - 2026-08-11

### Added
- Node 내장 테스트 러너 기반 인증, CSV, 백업, API, 페이지네이션, 버전 정합성 회귀 테스트 추가
- Pull Request와 main 브랜치용 CI 및 릴리스 테스트·빌드·보안 감사 게이트 추가
- 자산·이력 목록 페이지네이션과 검색 debounce, 조회용 SQLite 인덱스 추가
- 작은 화면용 반응형 레이아웃과 키보드 포커스 스타일 추가
- Go 런처의 PAM 헬스 계약 테스트와 Windows 교차 빌드 검증 추가

### Changed
- 관리자 비밀번호 저장을 단순 SHA-256에서 salt가 포함된 scrypt로 변경하고 기존 해시는 로그인 시 자동 마이그레이션
- 로그인 연속 실패 시 30초 동안 요청을 제한하도록 인증 보호 강화
- SQLite 온라인 백업으로 WAL 변경을 포함하고, 데이터 변경 후 자동 백업 및 최근 30개 보존 적용
- 자산/대여 상태 전이를 API 검증과 DB 트리거로 동기화하고 날짜 형식·순서 검증 강화
- React Router 7, Vite 6 및 관련 Node 의존성을 갱신하고 npm 감사 결과를 0건으로 정리
- 루트·클라이언트 패키지 버전을 `26.1.3`으로 통일하고 `launcher/go.sum`을 추적하도록 변경

### Fixed
- Excel에서 사용자 입력이 수식으로 실행될 수 있는 CSV Formula Injection 방지
- 목록 조회 실패가 빈 목록으로 표시되던 문제를 오류 메시지로 노출하도록 수정
- 폼 label 연결, 반납 모달 ARIA·Escape·포커스 복귀 등 접근성 문제 수정
- 다른 프로그램의 HTTP 응답을 PAM 중복 실행으로 오인하던 런처 헬스 판별 문제 수정
- 중복 실행 시 두 번째 런처가 트레이에 남던 동작을 브라우저만 열고 종료하도록 수정
- 백업 실패가 숨겨지던 문제를 API, 화면 경고 및 `/api/health` 상태로 노출

## [26.1.2] - 2026-05-18

### 추가
- 자산 관리 화면에 CSV 입력 양식 다운로드 기능 추가
- 자산 목록 CSV 가져오기 기능 추가
  - 필수 컬럼: `자산번호`, `모델명`
  - 선택 컬럼: `시리얼번호`, `상태`, `비고`
  - 기존 자산번호가 있으면 업데이트하고, 없으면 신규 등록
- 자산 목록 CSV 내보내기 기능 추가
  - 현재 검색어와 상태 필터가 적용된 목록을 내보내도록 지원

### 변경
- CSV 파일 처리 유틸을 자산 목록과 이력 목록에서 함께 사용할 수 있도록 확장
- CSV 가져오기 요청을 처리할 수 있도록 JSON 요청 용량 제한을 5MB로 조정
- README에 자산 CSV 가져오기/내보내기 사용법 추가

## [26.1.1] - 2026-05-18

### 변경
- 초기 관리자 비밀번호를 `admin1234`에서 `password1!`로 변경
- 기존 기본 비밀번호 해시(`admin1234`)를 사용하는 DB는 서버 시작 시 `password1!`로 자동 마이그레이션하도록 개선
- 최초 로그인 시 관리자 비밀번호 변경 모달을 강제 표시하도록 변경
- 비밀번호 변경 전 주요 업무 API 접근을 차단하도록 인증 흐름 보강
- 비밀번호 변경 규칙 안내 및 검증 추가: 최소 8자리, 대문자, 숫자, 특수문자 포함
- 시스템 트레이 정상 상태 아이콘을 초록색 사각형에서 PAM 로고 아이콘으로 변경

### 추가
- `POST /api/auth/change-password` API 추가
- PAM 로고 기반 런처 트레이 아이콘 리소스 추가


## [26.1.0] - 2026-05-16

### 추가
- PAM.exe 더블클릭으로 시스템 트레이 아이콘 등장 및 브라우저 자동 오픈
- 중복 실행 방지 (포트 3000 이미 사용 중이면 브라우저만 오픈)
- Node.js 크래시 감지 시 트레이 아이콘 오류 상태 전환 및 재시작 메뉴 노출
- 자산 등록 / 수정 / 삭제 (반출중 자산 삭제 방지)
- 자산 검색 및 상태 필터 (보관중 / 반출중 / 폐기)
- 반출 등록 (보관중 자산 선택 → 반출자 정보 입력 → 자산 상태 반출중으로 변경)
- 반납 처리 (반출중 자산 목록 → 반납 모달 → 자산 상태 보관중으로 복귀)
- 전체 이력 조회 (검색 / 상태 필터 / CSV 다운로드)
- CSV 내보내기 UTF-8 BOM 포함 (Excel 한글 정상 표시)
- 대시보드 통계 카드 (전체 / 보관중 / 반출중) 및 최근 이력 5건
- 서버 시작 시 SQLite DB 자동 생성 및 자동 백업
- 단순 비밀번호 인증 (SHA-256 해시, 세션 8시간)
- 폴더 복사만으로 다른 PC에서 동일하게 실행 가능한 포터블 구조
